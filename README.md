# selenium-cricbuzz

In this project, we use selenium toll. 
This ia an automation project wherein we fetch data from www.cricbuzz.com using matchId as unique reference. 
Then we store the data as a nested object in json file.

The ipl.js is writtent o fetch data of IPL matches.
The fetched Data is stored in career.json file.